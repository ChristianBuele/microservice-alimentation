spring:
  application:
    name: aliments-service
  cloud:
    config:
        uri: https://config-service-server.herokuapp.com
        username: root
        password: s3cr3t
  boot:
    admin:
      client:
        url: http://localhost:8086